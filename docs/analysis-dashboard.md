# MoneyHome8 分析仪表板

本文档基于 [analysis-metrics.json](C:\DCG-SZ\SZ-System-Docs\CodexWorkSpace\Finance-own\docs\analysis-metrics.json) 汇总当前分析包的量化状态，用于快速查看：

- 目前已经实测了多少条
- 目前高可信但未实测的有多少
- 当前还剩哪些明显缺口

数据基线：`2026-07-28`

## 1. 总量概览

当前统计结果：

- `实测`：`51`
- `高可信`：`184`
- `待验证`：`10`

当前结论：

- 我们已经拿到大量结构级、资源级和数据级证据。
- 页面级实测仍然少于结构级证据，这也是当前最主要的不平衡点。

## 2. 分区概览

### 顶层工作区

- `实测`：`3`
- `高可信`：`1`
- `待验证`：`0`

结论：

- 四大工作区里，`财务数据 / 财务报表 / 财务分析` 已有页面级证据。
- `记账` 仍停留在结构强推断阶段。

### 财务数据工作区

- `实测`：`22`
- `高可信`：`18`
- `待验证`：`0`

结论：

- 当前是页面级证据最充分的工作区。
- 账户中心已经可作为 Rust Phase 1/2 的优先实现页。

### 财务报表工作区

- `实测`：`20`
- `高可信`：`6`
- `待验证`：`1`

结论：

- 报表中心结构和全部 `25` 张业务报表已逐页动态确认。
- `23` 张生成真实表格或图表，网贷和银行理财 `2` 张在默认条件下返回明确无数据状态；主要缺口已转为精确 SQL、收益率公式、筛选边界、导出和打印结果。

### 财务分析工作区

- `实测`：`6`
- `高可信`：`24`
- `待验证`：`3`

结论：

- 预算入口和空状态已有实测。
- 诊断/规划/目标已完成代表性动态页面验证；主要缺口转为全部公式、逐年算法、边界值和失败回滚。

### 记账工作区

- `实测`：`0`
- `高可信`：`12`
- `待验证`：`1`

结论：

- 这是当前 UI 层最明显的空白点。
- 但它同时也是结构证据最扎实、最适合优先补证的工作区之一。

### 债权债务与信用

- `实测`：`0`
- `高可信`：`19`
- `待验证`：`0`

结论：

- 结构与资源证据已经很多，但页面与样例数据几乎没有。

### 投资与扩展资产

- `实测`：`0`
- `高可信`：`68`
- `待验证`：`0`

结论：

- 这是功能面最宽、结构最复杂的域。
- 当前已有大量对象与缓存/参考库证据，但缺页面和正式主表数据。

### 导入导出与辅助工具

- `实测`：`0`
- `高可信`：`13`
- `待验证`：`0`

结论：

- 能力存在感很强，但几乎都还没拿到真实交互细节。

### 同步、提醒、外围入口

- `实测`：`0`
- `高可信`：`13`
- `待验证`：`0`

结论：

- 协议/配置层清楚，页面层与真实运行层仍薄弱。

## 3. 当前最关键的不平衡

### 不平衡 1：页面证据少于结构证据

表现：

- 结构线索、数据源线索、缓存线索很多
- 页面级实测主要集中在三个工作区

影响：

- 足够开始实现
- 但不足以宣称已经完全复刻 UI 行为

### 不平衡 2：主账本认证未打通

表现：

- `test.mh8` 可被 Access COM 打开
- 但对象集合不可见

影响：

- 还不能正式枚举主账本表结构
- 仍需保留大量模式假设与结构推断

## 4. 当前阶段 readiness

### 架构设计

- `高`

理由：

- 工作区结构
- 数据源分层
- 领域模型
- 模块拆分
- 实施 backlog

都已经具备。

### Rust Phase 1

- `高`

理由：

- 账户中心
- 基础资料
- 参考库
- 缓存层

都已经有足够证据支撑先开工。

### Rust Phase 2

- `中高`

理由：

- 记账动态菜单和页面截图仍待补充
- 但运行时控件、字段、事件和实体链已经足以直接设计核心工作区

### 旧格式深兼容

- `低`

理由：

- 主账本认证和正式表结构尚未拿到

## 5. 当前最优先的补证顺序

1. 诊断、规划、投资收益率和报表公式 -> 代表性数据结果
2. `记账` 动态下拉、跳转和关键页面截图
3. `test.mh8` 正式结构与样例 -> 旧库迁移映射
4. 报表排序钻取、筛选边界、导出格式和打印结果

## 6. 对后续的直接建议

- 现在已经不需要再补更多“广度”文档，广度已经够了。
- 后续更高价值的是验证真实计算结果、动态交互和旧库迁移映射。

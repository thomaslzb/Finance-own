# MoneyHome8 功能审计清单

本文档把当前已发现的功能域整理成一个可审计清单，方便后续快速判断：

- 哪些功能已被真实页面证明
- 哪些功能只有结构/资源/配置证据
- 哪些功能还需要继续补证

状态说明：

- `PASS`
  - 已有足够强的当前证据支撑
- `PARTIAL`
  - 已知存在，但页面、结构或流程只拿到一部分
- `OPEN`
  - 已有方向，但关键证据仍明显不足

## 1. 顶层工作区

| 功能域 | 状态 | 当前证据 | 主要缺口 |
| --- | --- | --- | --- |
| 财务数据 | `PASS` | 顶部导航、左侧工作区结构、账户中心真实页面 | `概况 / 财务记录 / 投资一览 / 标签` 的真实内容仍待补充 |
| 财务报表 | `PASS` | 顶部导航、左侧分组、25 张运行时报表及公共筛选 | 查询结果列、公式和投资类页面截图 |
| 财务分析 | `PASS` | 顶部导航、预算真实创建/金额/滚动导入/执行率/冷启动，以及诊断/规划/目标运行时控件与字段 | 诊断/规划/目标公式、预算其它周期与收入路径的真实结果 |
| 记账 | `PASS` | 顶部入口、新增交易动作、录入/转账/分拆/模板/计划/流水运行时证据 | 动态菜单、跳转和真实写入结果 |

## 2. 账本与系统

| 功能域 | 状态 | 当前证据 | 主要缺口 |
| --- | --- | --- | --- |
| 新建账簿 | `PARTIAL` | 快捷键、窗体资源、生命周期文档 | 真实建账流程未实测 |
| 打开账簿 | `PASS` | 已用 `test.mh8` 打开软件，窗口标题可见 | 无 |
| 关闭账簿 | `PARTIAL` | 快捷键与配置存在 | 真实关闭行为未单独记录 |
| 备份账簿 | `PARTIAL` | 已真实生成加密 7z `.mh8k`，确认固定后缀、空名称校验、目录取消、成功提示、同名覆盖确认和覆盖否零写入；同一样本已由旧程序成功覆盖还原并冷启动 | Rust 解密迁移、附件/设置与全量业务内容比对、覆盖是备份失败及失败恢复未测 |
| 还原账簿 | `PARTIAL` | 已确认空来源 `00701`、文件选择取消、当前账簿覆盖确认取消、真实覆盖成功、业务摘要、重新打开和冷启动 | 新账簿目标、损坏/不兼容包、磁盘不足、中断失败回滚、自动安全备份和全量内容比对未测 |
| 结算账簿 | `PARTIAL` | 已确认默认截止日、结算恢复点保存取消、真实加密恢复点、先备份后写入、成功提示、23 条原记录变为 78 条余额调整、汇总不变和冷启动 | 日期边界、重复结算、全部账户/币种/持仓/债权债务/计划口径、恢复点还原和失败回滚未测 |
| 系统设置 | `PARTIAL` | 快捷键与窗体资源存在 | 页面内容未实测 |
| 快捷键管理 | `PARTIAL` | 专用窗体存在 | 页面内容未实测 |
| 密码校验/修改 | `PARTIAL` | 已实测无密码两字段、受保护三字段、空值清除、两次不一致、旧密码错误、空/错误密码重试、取消、正确冷启动解锁和清除后冷启动 | 旧格式 KDF、连续失败限速、边界密码策略、换密钥中断和失败恢复未测 |

## 3. 账户与基础资料

| 功能域 | 状态 | 当前证据 | 主要缺口 |
| --- | --- | --- | --- |
| 账户中心 | `PASS` | 真实页面、账户树、按钮、汇总口径；账户组精确重名、空白绕过缺陷、名称/详细资料修改、成员加入和冷启动；单账户 `Halifax -> CASH` 组归属取消、保存、即时刷新、同会话读回和冷启动；自定义导航全选/反选、单项显示、上移/下移、取消、保存和双冷启动；现金账户编辑取消、必填/重名、名称备注保存、排序和冷启动；非空组删除；四类显示开关、隐藏/恢复、注销/恢复、标签、非零账户永久删除及附件生命周期均实测 | 导航偏好作用域、空组/隐藏/注销/到期组合、并发失败；组归属解除、内联新建、组失效和批量迁移；组名大小写/Unicode/超长边界、日期/所有者修改、多币种/负余额注销，多成员/组详情原子删除，复杂关系永久删除和主账本正式表结构未枚举 |
| 开户向导家族 | `PARTIAL` | 新增账户类型目录五个分区、全部直接入口和七个展开组已实测，并建立到主要向导的路由矩阵；公共向导宿主已确认只有提示区和上一步/下一步/完成/取消协议，无独立业务页；现金向导已闭环两页默认值、必填/重名、草稿往返、标题栏取消、直接和内联成功、冷启动及提交后崩溃边界；活期一本通已闭环三页默认值、组/子账户校验、整体取消、组合提交、冷启动层级、小计和期初事件；普通活期已闭环两页默认值、名称校验、草稿往返、取消、无组无来源提交、即时投影、冷启动期初流水和正常退出；定期一本通已闭环四页默认值、组/子账户校验、期限利率联动、自动续存输入、草稿往返、取消、整存整取提交、到期计算和冷启动；多类向导另有创建/取消或页面证据 | 房产/汽车/其它重大资产参数化映射，无账簿/不可用类型，剩余向导字段、校验、真实提交和失败回滚；公共宿主异步提交、错误聚焦和键盘可访问性；现金、活期和组合账户的非人民币、所有者、真实资金来源、日期金额边界与失败注入；一本通多子账户和删除生命周期；定期其它存款类型、年存期、自动续存回填和完整到期生命周期；特定菜单关闭访问冲突的人工复现 |
| 资料管理宿主 | `PASS` | 当前运行态 11 个菜单入口、金融产品 10 类左侧路由、收支/人员/利率类型切换、关闭返回、同会话默认值和冷启动默认值均已实测 | 权限/许可显隐矩阵、无账簿状态、键盘焦点、窗口状态和跨类型未保存草稿策略 |
| 账户选择器 | `PASS` | 已在工资和转账入口实测单账户、多账户、搜索、无结果、单选回填、多账户零金额校验、字段级 `<无>`/新增配置、内联新增取消返回，以及来源/目标/手续费候选差异 | 键盘焦点、状态过滤矩阵、有效多账户金额、内联新增成功、Unicode 搜索、并发候选失效和失败回滚 |
| 收支项目选择器 | `PASS` | 已在日常收支宿主实测支出/收入候选、父子布局、跨方向搜索、空结果、收入/支出回填、失焦取消和内联新增取消，并确认收入上下文新增仍默认支出的旧版缺陷 | 管理入口可见宿主、内联新增成功、隐藏/系统/停用过滤、父项选择、真实键盘和并发失效 |
| 标签选择器 | `PASS` | 已在日常收支宿主实测无标签空态、过滤/无匹配、多选、确定、失焦回填、清空、内联新增空值校验、分号批量创建、取消及父级取消后的主数据保持，并与账户标签外层关系提交证据交叉验证 | 真实键盘、隐藏/范围过滤、Unicode 和大量候选、最大数量、候选版本失效、批量失败与交易标签真实提交 |
| 分类 | `PARTIAL` | 收入/支出列表、搜索、一级/二级展示、空名称和精确重名提示、同方向父级候选、父子真实创建、保存并新添、默认/自定义排序、跨项拖动、系统类别显示及冷启动保持均已实测 | 项目级编辑、真实隐藏/恢复、删除与引用保护、使用量排序口径、父子拖动边界、大小写/Unicode/空白规范化、并发失败和正式表结构仍待确认 |
| 标签 | `PARTIAL` | 空态/有数据态、单个与分号批量新增、空值/重复校验、改名取消/保存、无引用删除否/是、隐藏/取消隐藏、显示隐藏偏好及冷启动均实测；交易/资产双投影、查找筛选、批量空选择、导入记录结构、导出打印取消和导出取消误报已取证；账户关联/解除及标签排序另有动态证据 | 引用中标签删除保护、有记录批量加入/移出/转移/导入、真实导出打印、设为首页作用域、真实鼠标拖放持久化、名称规范化/并发/失败回滚和正式表结构仍待确认 |
| 人员/机构 | `PASS` | 已实测三类路由和差异化列；编辑器已完成字段显隐、生日历法、空值、取消、新增、修改和冷启动；列表已完成跨类型全局重名、显示字段搜索、隐藏/恢复、无引用删除、引用阻断及导出打印取消边界 | 名称空白/大小写/Unicode 规范化、隐藏或删除后名称复用、跨类型修改、真实 HTML 内容、实际打印、并发和失败回滚 |
| 币种 | `PARTIAL` | 已实测七个内置币种、本币标记、名称、代码和人民币牌价；CNY/CHF 修改删除禁用，页面无新增入口；CHF 设置为本币无确认即时提交并冷启动保持，人民币牌价列不变 | 自定义币种来源与编辑器、代码唯一性、引用删除保护、本币切换跨模块影响、在线牌价、现汇/现钞、导出打印和失败回滚 |
| 汇率 | `PARTIAL` | 已真实修改 `2026-07-29 USD/CNY 6.7894 -> 6.7895` 并冷启动；删除取消/确认、回退到 `2026-07-28 / 6.7829`、按日期跨币种视图均已实测 | 新增选择器绑定、重复业务键、数值边界、在线覆盖、历史估值影响、导出打印和并发失败 |

## 4. 通用交易与流水

| 功能域 | 状态 | 当前证据 | 主要缺口 |
| --- | --- | --- | --- |
| 收入录入 | `PASS` | 账户、项目、金额、日期、主题、备注、分期控件已确认 | 真实写入结果 |
| 支出录入 | `PASS` | 与收入共用收支编辑框架，字段和事件已确认 | 真实写入结果 |
| 转账录入 | `PASS` | 转出/转入、币种、手续费和手续费账户已确认 | 原子写入与汇率结果 |
| 现金/活期/第三方流水 | `PASS` | 页面标题、交易框架和字段已确认 | 代表性数据截图 |
| 流水账/日记账 | `PASS` | `TWasteBookFm` 字段、菜单和批量命令已确认 | 查询结果与打印/导出格式 |
| 模板/计划 | `PASS` | 批量收支/转账、生成规则、计划和自动执行控件已确认 | 调度与失败重试结果 |
| 常用备注 | `PARTIAL` | 列表、空内容提示、新增、原位修改、删除取消/确认和冷启动清理均已实测 | 重复/空白规范化、多行/Unicode/超长、引用中删除、导出打印、并发和失败回滚 |
| 存款利率 | `PARTIAL` | 人民币/外币矩阵、人民币单元格提交、冷启动持久化、在线更新和历史行情范围已实测 | 外币编辑、数值边界、版本生效、网络/部分失败回滚、导出打印和并发覆盖 |

## 5. 债权债务与信用

| 功能域 | 状态 | 当前证据 | 主要缺口 |
| --- | --- | --- | --- |
| 债权债务流水 | `PARTIAL` | 窗体族、对象族、统计页存在 | 样例数据与页面未实测 |
| 信用卡 | `PARTIAL` | 账户表、统计页、提醒窗体存在 | 页面与账单逻辑未实测 |
| 应收应付 | `PARTIAL` | 页面入口和窗体族存在 | 表结构/页面细节未拿到 |
| 预付待摊 | `PARTIAL` | 资源窗体存在 | 数据流细节未实测 |

## 6. 投资与扩展资产

| 功能域 | 状态 | 当前证据 | 主要缺口 |
| --- | --- | --- | --- |
| 外汇 | `PARTIAL` | 货币兑换 `9.00 CNY -> 1.00 GBP` 与外汇买卖 `6.00 CNY -> 0.88 USD @ 0.1473` 均已真实保存并冷启动；货币兑换确认稳定账户 ID、确定/续记共享校验和完整静态校验顺序；外汇买卖确认直接 `0.1473`、反向 `6.7894`、往返不交换交易腿，CNY/CNY 归一为 `1.0000`，零金额提示“卖出金额必须大于零” | 货币兑换同账户/同币种、零负金额和续记成功；外汇买卖反向保存/冷启动、正金额同币种和手工覆盖；汇兑损益、修改删除、余额不足和失败回滚 |
| 证券 | `PARTIAL` | 账户、资料、持仓、交易和估值页面已覆盖；全局 A/B 股费率已实测手工提交、冷启动及在线 11 条整批覆盖 | 账户级费率、费用舍入、成本批次、代码迁移、导出打印和失败回滚 |
| 开放式基金 | `PASS` | `_4` 类缓存、对象、窗体、参考价格 | 页面未实测 |
| 债券/国债 | `PARTIAL` | 窗体、缓存、参考价格线索 | 页面未实测 |
| 期货 | `PARTIAL` | 对象、窗体家族存在 | 页面/样例未实测 |
| 黄金/贵金属 | `PASS` | `ObjType=10`、对象表族、窗体存在 | 页面与主表正式数据未打通 |
| 融资融券 | `PARTIAL` | 融资/融券对象和窗体存在 | 页面未实测 |
| 保险/社保 | `PARTIAL` | 对象和窗体存在 | 页面未实测 |
| 重大资产 | `PARTIAL` | 买入默认值、名称校验、零金额兼容边界、`1.00 CNY` 无支付账户和 `10.00 CNY` 绑定 `Cash-CNY` 保存；全额卖出两项校验、`1.50 - 1.00 = 0.50`、绑定账户部分卖出 `6.00 - 4.00 = 2.00`、同日市值调整；追加投资零金额事件、`2.00` 联动、`3.00` 仅成本、`5.00` 绑定 `Cash-CNY`、`2.00` 新增贷款融资、`1.00` 未关联既有贷款复用和 `3.00=现金2.00+贷款1.00` 组合融资已保存，确认来源按差额分配、复用不重复本金、同一资产可保留多笔贷款，已关联既有贷款静默无操作已复现；收益与费用均已验证零金额阻断、`0.50` 外部路径和 `0.25` 绑定 `Cash-CNY` 路径；市值已验证零值、跨日新增、同日 upsert、修改、无确认删除、上一日期回退和冷启动；交易/估值/构成/概况/现金账户/负债/账户中心/全局流水投影已实测 | 多贷款分摊、已关联贷款候选与额度规则、非人民币、成本不等于市值的部分卖出比例和舍入、收益/费用扩展行为、市值唯一快照删除、日期/金额/币种边界、收益率、并发和失败回滚 |

## 7. 预算、提醒、规划、目标

| 功能域 | 状态 | 当前证据 | 主要缺口 |
| --- | --- | --- | --- |
| 预算 | `PARTIAL` | 已真实创建月度预算并验证额度、执行率、滚动导入和三类复制；分类移除会删除额度、重新加入从零，收入与支出独立投影且净额另算，全部已冷启动复核 | 季度/年度/自定义期间、非零实际收入执行率、父子分类/迁移/退款、多币种、有效但无来源年度、父级取消、导出、并发和失败回滚 |
| 提醒/财务日历 | `PARTIAL` | 财务日历当前日期、公历农历、账务标记、年月范围/切换和只读状态已实测；提醒窗体和远程通知链存在 | 提醒真实保存/触发/执行/跳过；日历提醒、生日、日记样例和详情提示 |
| 财务规划 | `PARTIAL` | 家庭/配偶、当前财务与未来事件专题、账户选择和年度区间已确认；年工资 `120000`、年支出 `60000` 的零增长样例每年净增 `60000` 且冷启动保持 | 起始余额、通胀/退休/资产增长复利顺序、分期购置、跨币种/舍入/负值、清除确认和失败回滚 |
| 财务目标 | `PASS` | 增删改、日期金额、账户绑定和余额/市值已确认 | 进度计算与真实样例 |
| 财务诊断 | `PASS` | 两种收支来源、八项手工输入持久化、定期/货币基金资产性质、十二项指标公式/参考区间和冷启动已确认 | 自动统计范围、零分母、阈值端点、并发和失败回滚 |

## 8. 导入导出与辅助工具

| 功能域 | 状态 | 当前证据 | 主要缺口 |
| --- | --- | --- | --- |
| 导入数据/分类 | `PARTIAL` | 窗体存在 | 文件格式与字段映射未实测 |
| 导入交割单 | `PARTIAL` | 专用窗体存在 | 字段映射规则缺失 |
| 导出数据 | `PARTIAL` | 专用窗体存在 | 输出格式未实测 |
| 附件 | `PASS` | 账户附件空态、添加、`test/Account/23` 受管副本、哈希、预览、系统打开、文件夹、动态菜单、双冷启动、删除否/是和空目录清理均实测 | 多附件、共享引用、二进制/大文件、故障回滚、账户删除组合及交易附件传播仍待确认 |
| 支票簿/票据 | `OPEN` | 窗体存在 | 页面、存储位置和关联方式未确认 |
| 工资收入/充值/净价计算 | `PARTIAL` | 长尾窗体存在 | 页面与流程未实测 |
| 证券代码转换/新股记录 | `PARTIAL` | 长尾窗体存在 | 页面与流程未实测 |

## 9. 同步、通知与认证

| 功能域 | 状态 | 当前证据 | 主要缺口 |
| --- | --- | --- | --- |
| 登录/注册 | `PARTIAL` | B18 已实测同步注册字段和空邮箱校验；数据库驱动登录框已明确排除 | 未使用真实凭据注册，旧服务结果未验证 |
| 同步对象链路 | `PARTIAL` | B18 已实测双向/单向、条款门禁、自动同步和删除确认，静态步骤链清晰 | 真实对象结果、冲突、删除传播和断点续传未验证 |
| 远程通知链路 | `PARTIAL` | B18 已实测手机快查开关即时保存和身份复用，静态通知步骤清晰 | 真实移动端查询、提醒接收和远端开关未验证 |
| 行情更新链路 | `PARTIAL` | B18 已实测九类最新、七类历史、外汇 `28` 条成功和股票中止；存款利率与证券费率已分别完成成功更新，证券费率会整批覆盖 11 条模板 | 其余来源、失败重试、历史批次、费率差异确认和供应商协议未验证 |
| 主账本认证链 | `PARTIAL` | `mh.mdw` / `user.cfg` / `UseInformation.cfg` / Jet 错误行为已明确 | 用户/口令/派生上下文未取得 |

## 10. 当前总结论

- 功能覆盖：`高`
- 数据流清晰度：`高`
- 页面实测完整度：`中`
- 主账本正式结构掌握度：`中偏低`
- 已可支持 Rust Phase 1 / Phase 2 开工

# 未解缺口登记

本文档只记录当前真正还没被打透的缺口，供后续继续推进时直接查看。

## 1. 数据层缺口

### G1 主账本认证

- 对象：
  - `test.mh8`
- 当前状态：
  - 已确认为 Jet 主账本
  - `Access.Application.OpenCurrentDatabase(...)` 已可返回成功
  - 但 `AllTables / AllQueries / TableDefs / QueryDefs / AllForms / AllReports / Properties` 仍为 `0`
- 缺口：
  - 用户/口令/派生认证上下文
  - 对象可见性/容器访问权限

### G2 主账本正式结构

- 对象：
  - `test.mh8`
- 当前状态：
  - 表名/字段/索引线索很多
  - 但没有正式 `SELECT` / `GetSchema` 结果
  - Access COM 已能开库，但对象集合为空
- 缺口：
  - 正式表结构
  - 样例数据
  - 主外键关系

### G3 内置库正式结构

- 对象：
  - `MoneyHome8.data` 解压库
- 当前状态：
  - 已确认是受控 Jet 内置库
- 缺口：
  - 正式认证
  - 正式表枚举

## 2. UI 与运行结果缺口

### G4 记账工作区

- 当前状态：
  - 顶部入口和 `TMainForm.btnAddTrans` 已直接确认
  - 通用收支、转账、分拆、模板、计划、专项交易页和财务记录控件已从运行时 DFM 直接确认
  - 财务记录命令、`Ctrl+C / Ctrl+V / Ctrl+Up / Ctrl+Down / Delete` 和批量模式动作已直接确认
  - 普通收入和普通支出共用 `TIncExpDlgFm + TIncExpEditFrame`；字段加载、取消无写入，以及 `其它收入 +3.00 CNY`、`教育培训 -2.00 CNY` 的真实保存、余额/分组/全局流水和冷启动持久性已实测
  - 记账三级菜单已通过 `WM_INITMENUPOPUP` 完整生成 22 个业务分组；钱包分组位置 `3` 的命令 `135/136` 已映射为充值/提现，充值初始、取消和零手续费真实保存路径已实测
  - 债权债务上下文菜单已确认债权/债务分组，债权八个命令、未报销记录查看和报销初始/取消路径已实测
  - 同币种转账本金 `50.00`、来源手续费 `1.00` 的真实保存、账户余额、来源流水、财务记录和冷启动持久性已实测
  - 取款本金 `4.00`、来源手续费 `1.00` 的真实保存、两侧余额、账户组、净资产、财务记录和冷启动持久性已实测
- 剩余缺口：
  - 更多交易活动中尚未展开的产品域子菜单、命令启用条件和上下文差异
  - 除已验证日常收入、日常支出、取款和同币种转账外的新增，以及保存并继续、标签/附件、分期、校验失败、修改删除和失败回滚
  - 取款的全部取完、超额利息、第三手续费账户、余额版本冲突和故障回滚
  - 其它已到达专用编辑器的真实保存结果，以及模板、计划和专项交易入口的动态结果

### G5 财务报表投资类子项

- 当前状态：
  - 三个分组和全部 `25` 张业务报表已逐页查询并形成截图
  - `12` 张投资类子报表均已到达；证券/基金趋势图、投资收益率和持仓盈亏表已有真实结果
  - 网贷和银行理财在默认条件下返回明确无数据状态
  - `38` 个交易/统计窗体字段、`20` 个计算字段、`6` 条求和规则和 `2` 条页脚绑定已结构化提取
  - 公共筛选页签、两种收益率查询模式、操作菜单、导出/打印初始禁用和趋势默认序列已确认
- 剩余缺口：
  - 精确 SQL、收益率/年化收益率和投资盈亏公式
  - 排序、钻取、筛选边界、导出格式和打印预览
  - `TUsableMoneyChartFrame` 在概况页的独立交互

### G6 财务分析子页

- 当前状态：
  - 月度预算已真实创建并冷启动，确认预算头与分类范围的两步提交、十二个月额度持久化、实际/预算投影和月份切换
  - 最近 12 个月导入已确认读取执行日前一个完整月份结束的连续 12 个月，并按月份编号覆盖目标预算年度；零预算分支和整数执行率公式已校准
  - 分类移除已确认删除该分类全部月份额度，重新加入从零初始化；收入 `17.89` 与支出 `43,660.70` 独立展示，网格净额 `-43,642.81`，并已冷启动复核
  - `财务诊断 / 财务规划 / 财务目标` 已形成独立页面截图和运行态记录
  - 财务诊断两种收支来源、八项手工输入冷启动持久化、十二项公式/参考区间和资产性质等额重分类已确认
  - 规划家庭/配偶资料、收入、支出、资产、债务、保险、通胀、退休、教育、重大支出、资产购置和年度余额图已确认；工资 `120000`、年支出 `60000` 在零增长下每年净增 `60000` 并冷启动保持
  - 目标创建、必填校验、账户绑定、进度展示、删除确认和清理后空状态已确认
  - 财务日历默认当前日期、公历农历、账务记录标记、十二个月份、`2026..1990` 年份范围、年月切换和只读重开已确认
- 剩余缺口：
  - 预算季度/年度/自定义期间、非零实际收入执行率、父子分类迁移/退款、多币种、有效但无来源年度、父级取消、导出、并发和失败回滚；复制、分类移除/重新加入和收支分区规则已闭环
  - 财务诊断自动统计日期范围、退款/冲销、跨币种和缺失估值；零分母、负数、阈值端点、并发和失败回滚
  - 规划第一个年度余额的组成、通胀/退休/资产增长复利顺序、分期购置逐年展开、跨币种/舍入/负值和清除确认
  - 目标标准/实际进度的边界、舍入和多账户估值口径
  - 保存失败、并发变化和事务回滚
  - 日历中提醒、生日、日记的动态样例、单条事务提示、多记录排序和计划状态变化

### G7 财务数据剩余子页

- 当前状态：
  - `概况 / 财务记录 / 投资一览 / 标签` 已确认存在
  - 真实页面内容仍缺

## 3. 协议层缺口

### G8 缓存协议细节

- 当前状态：
  - `_PY / _LIST / _3 / _4 / _9` 已有高可信语义
- 缺口：
  - 记录分隔规则
  - 完整字段布局

### G9 导入导出映射

- 当前状态：
  - 整账簿导入导出共同覆盖 21 类数据集
  - 旧版交换 XML 已确认 `.xml`、`GB2312`/代码页 936、CRLF、`INOUTDATA VERSION=1.21` 和 14 个全量实际数据段；全量导出存在长时间无反馈但后台继续完成的问题
  - 通用账户流水已确认内置来源、`csv/xls/xlsx/htm/html/txt` 文件筛选器、原始数据和规范化预览；预览阶段会修改旧账簿文件，是明确缺陷
  - 交割单 12 类交易、10 字段、分隔符、映射方案、预览、含费选项和批量修正已确认；系统方案运行态默认空格分隔，一条买入记录已成功进入预览
  - 旧程序回导自己成功导出的货币 XML 时发生访问冲突并退出，Rust 版必须结构化报错并保证原子回滚
  - 导入批次、逐行错误、幂等和覆盖范围的 Rust 开发契约已固定
  - 报表导出代码确认 `.htm/.html`、`.xls` 和 `.pdf`；剪贴板导入出现 `.html`、`.csv` 和 `._clip` 线索
  - 备份代码和真实操作均确认 `.mh8k`、空名称校验、目录选择取消、完整路径覆盖确认及覆盖否零写入；成功样本是包含加密 `BOOK` 条目的 7z 容器。该样本已由旧程序完成当前 `test.mh8` 覆盖还原、成功重开、业务摘要抽查和冷启动；代码存在新账簿名线索，但当前可见入口未显示目标切换
- 缺口：
  - 21 类选择与 XML 节点的完整映射、成功回导、覆盖范围、重复检测和逐段错误
  - 银行/信用卡专用来源格式、Rust 端 `.mh8k` 解密与内部内容映射、附件/设置和全量业务内容比对、真实回导结果
  - 交割单日期、负数、千分位、空值与去重的动态规则
  - 信用卡账单和报表导出的实际格式

### G10 附件/票据存储

- 当前状态：
  - 附件支持发票、收银条、存折、照片、视频、网页截图和文档
  - 添加、删除、打开、打开目录以及旧路径依赖警告已确认
  - 新系统采用受管副本、相对路径、内容哈希和独立关联表
  - 删除附件前确认已由事件代码证明；新系统已固定“先解除关系、最后引用才删除内容文件”的规则
- 缺口：
  - 旧程序附件目录命名与复制/引用行为
  - 删除最后一个引用后的物理文件结果
  - 票据和支票簿的专项格式

### G11 特殊入口、计算器与嵌入视图动态结果

- 当前状态：
  - `460/460` 个运行时窗体已完成静态分类，未分类数为 `0`
  - `TAIPANELDLG` 被标记为实验 WebView，`TCONSOLEFM` 被标记为内部诊断窗口
  - `TCALCUFM` 已归入共享技术组件；全程序 `429` 个 `TMHCalcuEdit` 分布于 `160` 个窗体
  - 方法级证据已确认控制台热键为 `Ctrl+F12`，老板键空值回退为 `Ctrl+1`
  - 控制台三页共用 `Console.htm`；主控制台可输入，网络和 SQL 页只读，并注册 `25` 个命令类
  - AI 面板加载 `data/AIPanel.html`，三类旧接口均为明文 HTTP，发送用户输入、key、时间值和 MD5 签名
  - 计算器结果、错误、`Esc` 和方向上键均调用 VCL `TCustomForm.Close`
  - B19 已在日常收支金额字段通过 `F4` 打开 `TCalcuFm`，确认完整按键布局和共享宿主定位
  - B19 快捷键设置页动态显示控制台为只读 `Ctrl+F12`；当前自动化会话仍未打开可见控制台窗口
  - B19 未发现 AI 用户入口，且因旧明文 HTTP 风险没有发送请求
  - `37/37` 个无独立文案视图均已从 `107` 条逻辑直接组合关系中定位直接父窗体和最终宿主
  - `2332` 条 DFM 事件绑定已归并为 `2000` 个处理器，`1951` 个已定位当前类或真实父类代码
  - `TCASHWITHDRAWDLGFM`、`TIMPORTJIAOGEDANDLGFM`、`TRZFRMCUSTOMIZETOOLBAR` 的 `48` 个处理器只有资源绑定证据；当前已分别形成运行类对应、真实入口保留和第三方内部能力排除结论
  - AI 和控制台均无设计时父窗体引用，仍可能由事件代码动态创建
- 缺口：
  - AI 是否存在当前发行版未暴露的条件分支或隐藏命令；如需验证，只能使用无真实财务数据的隔离 HTTPS 替代端点
  - 控制台触发前置条件、`25` 个命令的语法、输出和真实副作用
  - 金额计算器结果回填、错误提示、焦点恢复和键盘关闭结果；输入框唤起已动态证明
  - 嵌入视图读取什么数据、应用什么筛选、在空数据和有数据时显示什么结果
  - Rust 版对每个表面的保留、合并或明确排除决策
  - B20 之后无 VMT 资源已不再缺少产品范围决策；剩余工作是验证取款运行类与资源字段完全对应，以及确认没有需要迁移的旧工具栏布局数据

### G12 同步、通知与行情协议

- 当前状态：
  - B18 已动态确认同步设置的上次同步时间、默认双向、单向上传、条款门禁、关闭账簿自动同步和账号删除确认
  - 同步注册页、手机快查开关及其注册入口已动态确认；手机快查开关会即时保存
  - 行情页已确认九类最新来源、七类历史价格源、三种标的范围和日期区间
  - 外汇牌价成功获取 `2026-07-29` 的 `28` 条记录；股票更新在写入前中止，未观察到账簿大小和修改时间变化
  - `TLoginDialog` 已判定为数据库驱动回退窗体，正常产品流程不可达，目标 Flutter UI 明确排除
  - 旧程序同步凭据可从 UI 自动化树读取，且条款、同步和密码入口使用 HTTP；两项均作为安全缺陷，不复刻
- 缺口：
  - 真实同步对象结果、冲突合并、删除墓碑传播、断点续传、取消和幂等重试
  - 旧服务是否仍可用及其响应协议；只能在无真实财务数据、无真实凭据的隔离环境中验证
  - 手机快查真实查询内容、提醒接收、远端开关和失败行为
  - 股票、港股、美股、基金、贵金属、期货、存款利率、交易费率及历史区间的成功、失败和重试结果
  - 行情供应商、校验规则、批次原子发布、旧参考库回写和多页面一致性

### G13 系统外壳与账簿生命周期成功路径

- 当前状态：
  - B01 的 `15/15` 个资源均已有最新结构化部分观察记录
  - 主窗口、七个顶层菜单、新建、结算、备份、还原、密码设置、系统设置、快捷键、软件许可和关于均已真实到达
  - 目标进程已正常关闭，另一 MoneyHome8 进程未受影响；结束时无账簿锁文件
  - 序列号、保护信息、通行证和用户资料均已脱敏，没有进入文档或新截图
  - 密码设置、两次不一致、受保护冷启动、空/错误密码重试、取消、正确解锁、旧密码错误和清除保护已真实验证；临时密码未落盘
- 缺口：
  - 真实新建成功、同名冲突、磁盘失败和创建后密码流程
  - 旧 `.mh8k` 已确认加密 7z 外层、单条 `BOOK` 和声明长度，并已由旧程序完成当前账簿覆盖恢复与冷启动；仍缺 Rust 解密、还原到新账簿、版本迁移、附件/设置和全量业务内容比对、损坏包与失败回滚
  - 结算已确认先生成加密恢复点，再把最近范围重写为同日余额调整，账户汇总不变；仍缺历史/未来日期、重复结算、全部账户、持仓、外币、债权债务、计划、附件和失败回滚口径
  - 旧格式 KDF 和密码迁移、连续失败退避/审计、边界密码策略、换密钥中断和半失败恢复
  - 设置持久化位置、即时生效规则、旧配置迁移、快捷键冲突和许可服务协议

### G14 账户与基础资料成功路径和引用保护

- 当前状态：
  - B02 的 `41/41` 个资源均已有结构化观察记录，现金、银行理财、家居物品、待摊费用、账户标签外层选择器和第三方储值编辑器等代表条目已达到 `pass`
  - 账户类型目录及现金、活期、组合账户、金融产品和第三方储值代表向导已完成真实提交、取消或冷启动验证
  - 分类、人员、标签、账户组、汇率和存款利率已有真实写入、编辑、删除或冷启动证据，具体剩余边界按各记录追踪
  - 每轮动态验证均只操作指定测试账簿，并在归档业务态后恢复账簿和恢复旁文件基线
- 缺口：
  - 未覆盖账户子类型、基础资料名称规范化边界、并发冲突、技术失败回滚和完整审计
  - 一卡通等组合账户的多成员、现有成员选择、定期子账户、主子实体原子失败回滚和删除边界
  - 期初余额从既有账户转入时的双边平衡分录、多币种口径和余额不足处理；外部期初已有现金、活期、理财、待摊及其它储值代表证据
  - 被账户、交易、预算或计划引用的基础资料删除、合并和替换保护
  - 汇率已补齐手工窗体的有序币种对、四位定点保存和完整校验顺序；仍缺真实新增绑定、同日期重复键、来源/现钞现汇、生效选择、反向查询、并发覆盖和批量更新规则。存款利率仍缺外币、生效日期和更多版本边界

### G15 交易提交、批量模板与分期闭环

- 当前状态：
  - B03 的 `38/38` 个资源均已有最新结构化部分观察记录
  - 财务记录、现金/活期/第三方账户流水、日常收支、取款、转账、工资和分拆页面已动态到达
  - 查找、筛选、仅收支/全部记录、操作菜单、批量记账、批量转账和概况收支图表已取证
  - 零金额模板行跳过、批量转账手续费从转出账户扣除及普通活期账户不显示分期入口的条件已记录
  - 单笔同币种转账已真实保存并冷启动复核：来源 `-51.00`、目标 `+50.00`、手续费净影响 `-1.00`，账户选择必须绑定稳定对象身份
  - 日常收入和日常支出已分别真实保存并冷启动复核：`Cash-CNY +3.00 CNY` 与 `Cash-CNY -2.00 CNY` 按显式交易方向生成单边分录，账户余额、账户组和全局流水逐分一致
  - 取款已真实保存并冷启动复核：`顺德农行 -> Cash-CNY` 本金 `4.00 CNY`、来源手续费 `1.00 CNY`，来源总流出 `5.00`、目标流入 `4.00`、净资产只减少手续费 `1.00`
  - 单笔货币兑换已真实保存并冷启动复核：`Cash-CNY -9.00 CNY`、`Cash-GBP +1.00 GBP`；全局记录币种列为空且旧页脚直接累加不同币种名义金额。补充失败验证确认确定/保存并继续共享“请选择换入账户”，只显示 Cash-GBP 但未绑定稳定对象仍被拒绝；静态处理器确认账户 ID 不同、币种不同和双金额正数顺序
  - 银行资金利息收入已真实保存并冷启动复核：`Cash-CNY +2.00 CNY`，账户和 CASH 分组同步增加 `2.00`，全局记录只有流入；旧界面对本位币隐藏币种文字，Rust 持久层仍须保存明确 CNY
  - 外汇入口其它费用已真实保存并冷启动复核：`Cash-CNY -1.25 CNY`，账户和 CASH 分组同步减少 `1.25`，全局记录只有流出；旧投影不显示外汇来源，Rust 必须显式保存来源上下文
  - 外汇买卖已真实保存并冷启动复核：`CodexFX-1706` 卖出 `6.00 CNY`、买入 `0.88 USD`、成交汇率 `0.1473 USD/CNY`；全局记录为单条“外汇买卖”双边金额，账户折算总值为 `99.97 CNY`
  - 外汇账户有数据工作区已补齐：统计显示 `0.88 USD / 6.7894 / 5.97 CNY` 与 `94.00 CNY / 1.0000 / 94.00 CNY`，合计 `99.97 CNY`；构成图图例复用两个折算金额。交易明细确认余额调整与外汇买卖两类真实行，并记录持仓范围、交易范围、日期范围、记账和操作菜单
  - 外汇买卖真实方向点击确认直接 `0.1473`、反向 `6.7894`、再次点击返回直接，CNY/USD 交易腿始终不交换；反向值不是已舍入直接显示的倒数，Rust 必须保留未舍入规范牌价。CNY/CNY 被选择器接受、归一为 `1.0000` 并隐藏方向链接，零金额提交提示“卖出金额必须大于零”。运行时处理器另确认双向金额联算、汇率变化后重算买入金额和六项保存校验
- 缺口：
  - 钱包充值已真实保存并冷启动复核：`Cash-CNY -> 微信钱包` 本金 `2.00 CNY`、手续费 `0.00`，来源 `-2.00`、钱包 `+2.00`、净资产不变，全局记录流入/流出各 `2.00`
  - 钱包提现已真实保存并冷启动复核：`微信钱包 -> Cash-CNY` 本金 `1.00 CNY`、手续费 `0.00`，钱包 `-1.00`、目标 `+1.00`、净资产不变，全局记录流入/流出各 `1.00`
  - 钱包提现手续费已真实保存并冷启动复核：本金 `1.00 CNY`、手续费 `0.10 CNY` 时，钱包 `-1.10`、目标 `+1.00`、净资产 `-0.10`，全局记录流入/流出 `1.00/1.10`，确认手续费由钱包源侧承担
  - 钱包充值手续费已真实保存并冷启动复核：本金 `1.00 CNY`、手续费 `0.10 CNY` 时，资金来源 `-1.10`、钱包 `+1.00`、净资产 `-0.10`，全局记录流入/流出 `1.00/1.10`，确认手续费由资金来源承担
  - 钱包充值手续费修改删除已真实验证：手续费 `0.10 -> 0.20` 只按差额重算来源和净资产，删除后本金与费用投影完整恢复并通过冷启动；提现已验证编辑关闭、删除否和确认删除，但费用修改差额仍待验证
  - 钱包充值与提现的“保存并继续”均确认保留钱包和日期，重置对方账户、金额、手续费、标签与备注；两个方向的编辑关闭和删除否均零写入。附件/标签/内联账户保留策略仍待验证
  - 余额调整已完成 `Cash-CNY` 默认类型生命周期、两个策略的零差额、默认与日常收支保存并继续，以及 `日常收支` 正负差额转换和通用收支编辑路由；日常收支续记已暴露陈旧账面与已提交字段保留缺陷。仍缺陈旧草稿不修改再次提交、完整报表口径、关闭或多币种账户、历史/未来日期、并发冲突与故障回滚
  - 工资收入已从 DFM 和 19 个事件处理器补齐收入、普通扣款、社保账户个人/公司缴费、实收汇总和税务计算器的数据流；动态校准已确认 `Cash-CNY` 账户候选绑定、四个社保人员候选、选择 `Lzb` 后展开社保三列表格、收入 `10.00` 的即时实收 `10.00`，以及缺少有效收入项目时阻止提交。仍缺真实分类与社保账户绑定、普通扣款/个人缴费/公司缴费的完整旧版公式、成功保存、税务回填、保存并继续、修改删除、报表投影和故障回滚
  - 钱包双向费用报表分类、提现费用修改差额、校验失败、并发冲突与事务回滚仍待真实提交
  - 通用日常收支的保存并继续、标签/附件、零负金额、禁用账户、修改删除、重复提交和故障回滚；此项不等同于余额调整中的“日常收支”转换策略
  - 取款的全部取完、超额利息、第三手续费账户、余额不足、同账户、修改删除、重复提交和故障回滚
  - 同币种转账的同账户、余额不足、第三手续费账户、并发冲突和故障回滚
  - 货币兑换的真实同账户/同币种错误焦点、零负金额、余额不足和保存并继续成功后的新草稿；外汇买卖的正金额同币种提交、反向报价金额联算与保存/冷启动、手工汇率和双边金额覆盖；两类换汇仍缺旧账本成交汇率快照、手续费、汇兑损益、修改删除和故障回滚。外汇工作区仍缺所有交易过币种中的零余额历史币种、查找、批量、导出打印、牌价刷新及零值/负值/缺失牌价构成规则。货币兑换稳定账户绑定与完整校验顺序、外汇买卖方向切换/同币种选择/零卖出金额均已有证据。外汇账户资料已补齐主档/扩展字段映射和固定币种契约，仍缺真实资料保存、银行引用、附件、并发和失败回滚
  - 费用/利息的零负金额、禁用账户、外币折算、其它投资产品域关联、修改删除、重复提交和故障回滚；当前只验证银行利息与外汇费用的 CNY 正常路径
  - 批量模板逐行校验、幂等重试、整批/有效行策略、逐行结果和重复生成保护
  - 分期协议、计划生成、首期入账、重算、提前结清和已入账期次保护
  - 修改、删除、退款、复制粘贴、改变类型、转计划、附件和批量操作的审计与引用保护
  - 财务记录、账户流水、底部汇总和概况图表在退款、跨币种和复杂手续费场景下的逐分一致性；旧版跨币种名义金额直接汇总缺陷不得复制

### G16 债权债务、信用卡与摊还闭环

- 当前状态：
  - B04 的 `54/54` 个资源均已有最新结构化部分观察记录
  - 债权债务明细、构成、还款表、概况、筛选以及应收、应付、预收投影已动态到达
  - 信用卡交易、分期、账户设置、账户概况、还款、单币/双币向导和网贷向导已有运行证据
  - 零余额临时信用卡已创建并永久删除；专用进程退出后业务筛选不再显示该账户
  - 待摊费用已完成创建、必填与期次校验、外部期初余额投影、概况取消与修改、删除警告和级联清理；活动账簿与恢复旁文件均已精确恢复
- 缺口：
  - 借入、借出、预收、预付、收回、返还、提前结清、坏账、垫付和报销的真实提交与回滚
  - 待摊费用选择真实资金账户时的双边初始分录、到期自动摊销、旧版尾差公式、已入账期次保护和冷启动计划重建
  - 本金、利息、手续费、罚息、奖励和坏账损失的逐分分配及合同版本并发保护
  - 信用卡账期归属、账单生成、还款分配、分期计划、提前结清和已入账期次保护
  - 网贷借出、收款计划、奖励、转让、坏账和收回的关联身份、状态流转及幂等重试
  - 旧程序恢复副本 `~$test` 仍在账簿目录；RT-02-036 已为账簿和旁文件建立双恢复点并在退出后恢复精确指纹，后续动态批次仍必须重复核对进程、锁、恢复副本和文件指纹

### G17 定期存款与银行理财生命周期

- 当前状态：
  - B05 的 `19/19` 个资源均已有最新结构化部分观察记录
  - 定期账户编辑、统计工作区、交易宿主、开户向导和账户概况已动态到达
  - 银行理财账户、产品目录、产品编辑、统计工作区、交易宿主和产品信息页已有运行证据
  - 零余额临时银行理财账户已创建并永久删除；未创建产品、持仓或业务交易
- 缺口：
  - 定期续存、正常到期、提前支取的真实事件、利息计算、资金分录和失败回滚
  - 银行理财产品创建、编辑、核销及已被持仓引用时的删除保护
  - 申购、赎回、分红、其它费用、利息收入和到期结算的真实提交、幂等与并发保护
  - 持仓本金、实际现金流、预期/实际收益及年化收益率在列表、详情和报表间的同快照一致性
  - `TCurrDepositsViewFrame` 与 `TMoneyProductsViewFrame` 仍只确认宿主组合边界，尚缺有数据状态下的动态内容

### G18 投资公共快照与盈亏闭环

- 当前状态：
  - B07 的 `6/6` 个资源均已有最新结构化部分观察记录
  - 投资一览已动态确认账户分组、持仓列、成本、市值和浮动盈亏合计
  - 投资构成图已确认按市值与按成本两种模式，图例合计与页面对应合计精确一致
  - 历史盈亏及市值构成和变动已在多个投资工作区最终宿主中到达
  - 持仓调整及共享费用/利息编辑器已有字段、模式和取消证据
- 缺口：
  - 真实持仓调整的数量/成本差额、版本冲突、审计和失败回滚
  - 卖出、赎回和平仓对应的成本批次分配、费用归属、实现盈亏与比例公式
  - 多币种持仓在成本、市值、浮动盈亏、图表和报表间的汇率批次一致性
  - 市值变动在当日净交易、缺失昨日行情和跨币种场景下的精确口径
  - 历史盈亏有数据状态下的筛选、排序、钻取、导出和打印

### G19 上市证券交易、费率与代码迁移闭环

- 当前状态：
  - B08 的 `21/21` 个资源均已有最新结构化部分观察记录
  - 证券账户向导、账户编辑/概况、证券与价格目录、全局 A/B 股费率和代码变更页已动态到达
  - 持仓统计、交易明细、市值构成和历史盈亏已确认真实宿主、字段和有数据状态
  - 买入、卖出、新股申购、中签确认、配股、送股/缩股和现金红利已有表单证据
- 缺口：
  - 账户级费率页的直接入口、保存、版本和全局模板覆盖优先级
  - 新股申购、中签、退款三记录的关联、部分中签和异常回滚
  - 独立证券选择对话框的候选过滤、在线更新刷新和返回语义
  - 真实买卖费用的税费单位、最低佣金、舍入、成本批次和已实现盈亏
  - 证券代码变更后的历史交易、持仓、行情、别名检索和报表一致性

### G20 开放式基金与货币基金交易闭环

- 当前状态：
  - B09 的 `29/29` 个资源均已有最新结构化部分观察记录
  - 两类账户向导、账户编辑/概况、基金目录、净值、持仓、交易、市值和历史盈亏已动态到达
  - 开放式基金样例已校准市值、均价、浮动盈亏和总盈亏之间的关系
  - 货币基金零余额临时账户已创建、覆盖空工作区并删除，账簿已恢复 B09 前指纹
- 缺口：
  - 两类基金申购、赎回和转换的真实金额/份额、费用、舍入、成本和失败回滚
  - 开放式基金前端/后端收费、申购/赎回费率来源和净值确认时点
  - 货币基金面值、收益结转、赎回利息和资金在途状态
  - 新基金认购、确认、返款及部分中签状态机
  - 基金拆分比例方向、零碎份额、成本和历史净值调整

### G21 债券交易、利息与兑付闭环

- 当前状态：
  - B10 的 `15/15` 个资源均已有最新结构化部分观察记录
  - 债券资料目录/编辑器、账户向导/编辑/概况、持仓、交易明细、成本市值构成和历史盈亏已动态到达
  - 买入、卖出、到期、提前兑取和利息五类专用表单已确认，正常到期与提前兑取保留为不同事件
  - 零余额人民币临时债券账户已创建、覆盖工作区并删除，账簿已恢复 B10 前指纹
- 缺口：
  - 债券类型、代码、发行人、付息日及已被交易引用后的修改/删除保护
  - 买卖净价、应计利息、全价、数量、费用、资金金额和舍入公式
  - 成本批次、卖出成本、已实现盈亏、市场估值和跨币种汇兑
  - 正常到期与提前兑取的本金、利息、溢折价、费用、税务及状态迁移
  - 利息支付、免税标志、失败回滚、导出与打印结果

## 4. 当前执行优先级

### P1 动态结果校准

- 按 [runtime-execution-queue.md](C:\DCG-SZ\SZ-System-Docs\CodexWorkSpace\Finance-own\docs\runtime-execution-queue.md) 继续关闭动态条目；外汇、现货贵金属、贵金属 TD、期货、融资融券、商业保险、社保、重大资产和家居物品已补充账户向导、交易编辑器、工作区、资料、价格或费率入口。重大资产已完成无支付账户买入、零金额兼容边界、绑定 `Cash-CNY` 的 `10.00` 买入内部资产转换、全额卖出 `1.50 - 1.00 = 0.50`、绑定 `Cash-CNY` 的部分卖出 `6.00 - 4.00 = 2.00`、同日市值调整、追加投资零金额事件、`2.00` 成本/市值联动、`3.00` 仅成本追加、`5.00` 绑定 `Cash-CNY` 内部转换、新增贷款融资 `2.00` 的资产/负债同额增加、未关联既有贷款 `1.00` 成功复用且本金不重复、`3.00=现金2.00+贷款1.00` 组合融资、已关联既有贷款静默无操作、投资收益和投资费用各自的 `0.50` 外部路径与 `0.25` 绑定 `Cash-CNY` 路径，以及市值零值、跨日新增、同日 upsert、修改、删除回退、跨页投影和冷启动复核；仍缺多贷款分摊、已关联贷款候选与额度规则、非人民币汇率、成本不等于市值时的部分卖出比例和舍入、收益/费用非默认类别、标签、保存并继续、市值唯一快照删除和日期/金额/币种边界、收益率、并发和失败回滚；家居物品仍缺真实买卖重估、分期买入、成本算法、引用保护和失败回滚
- 使用代表性 TD 开仓和平仓样例校准成交金额、手续费、保证金占用/释放、持仓批次、可用资金、已实现/浮动盈亏和收益率，并验证资金不足、超量平仓及事务回滚
- 使用代表性期货开仓和平仓样例确认合约形成规则，校准成交金额、手续费、保证金占用/释放、账户可用资金、已实现/浮动盈亏和收益率，并验证资金不足、超量平仓及事务回滚
- 使用代表性融资买入、融券卖出、卖券还款、买券还券、批量偿还和担保物划转样例确认合同形成与分配规则，校准本金或数量债务、利息、分项费用、成本、资金和盈亏，并验证超额偿还、版本冲突、重复提交及事务回滚。批量直接还款已用真实未结融资合同复现候选为空和空行提交阻断，下一步需先确认旧版候选筛选条件，再验证可成功选择的单合同/多合同批次
- 保险缴费、保费返还、保险分红和退保已用临时现金账户真实保存：缴费样例为期初 `100.00`、缴费 `10.00`、余额 `90.00`；返还和分红分别形成 `4.00`、`2.00` 领取账户流入；退保 `5.00` 已完成保留/终止账户对照。现金价值唯一快照删除、两日期删除最新值/非最新值、历史/未来日期、负数/空值、人民币舍入和大额金额均已验证。继续校准业务日期来源、跨月/跨年/时区、其它币种精度、科学计数法、千分位、本地化分隔符、精确上限、聚合溢出及读模型根因。还需复拍未终止分支的资金/财务记录交叉页，并验证非空草稿恢复、保费收支统计开关、候选过滤、后续命令、精确损益、计划处理、重复提交、并发、失败回滚和费用税务口径
- 按 [runtime-validation-scenarios.md](C:\DCG-SZ\SZ-System-Docs\CodexWorkSpace\Finance-own\docs\runtime-validation-scenarios.md) 执行命令、交易、外汇、投资、预算提醒和报表场景
- 补齐更多交易活动的剩余产品域子菜单、上下文命令和关键页面截图
- 验证诊断自动统计与边界；规划起始余额、复利和分期算法；目标多账户估值与进度边界
- 验证投资报表的动态列、成本/盈亏公式、聚合和导出格式
- 验证控制台可达前置条件、金额计算器回填/错误/焦点，以及各主要业务域嵌入视图在已确认父窗体中的动态内容和结果；AI 默认维持无入口和禁用外联决策
- 校准 B20 共享组件的日期/金额边界、键盘焦点、统计快照一致性、任务取消和 WebView 隔离
- 校准 B01 真实新建、还原到新账簿、旧备份 Rust 解密迁移、结算日期/重复范围/跨域口径和失败回滚、密码 KDF/迁移失败恢复、设置持久化及许可协议
- 校准 B02 账户与基础资料成功/失败保存、组合账户原子性、期初资金分录、引用保护和费率历史
- 校准 B03 单笔/批量交易、跨币种和手续费、分拆尾差、分期、退款审计、附件和失败回滚
- 校准 B04 债权债务合同、信用卡账单与还款、分期摊还、网贷计划、本息费用分配和失败回滚
- 校准 B05 定期续存/到期、银行理财产品生命周期、持仓本金、申购赎回、收益公式、删除保护和失败回滚
- 校准 B07 持仓调整、成本批次、实现盈亏、跨币种估值、市值变动和有数据输出
- 校准 B08 账户级费率、新股关系、证券选择、费用舍入、成本批次、代码迁移和失败回滚
- 校准 B09 真实金额/份额、申赎费率、收益结转、成本分配、认购、拆分和失败回滚
- 校准 B10 真实净价/全价、应计利息、费用、成本批次、到期/提前兑取损益、税务和失败回滚

### 并行迁移线

- 打通 `test.mh8` 正式认证并取得只读表结构和样例
- 建立旧标识到 SQLite 新实体的迁移映射
- 不让旧库认证阻塞新主库和 Rust 高频功能闭环

### P2 长尾协议

- 继续细化缓存协议
- 继续确认 21 类 XML 节点映射、成功回导、银行/信用卡专用格式和报表导出内容；通用 XML 与 CSV 预览已取证
- 动态确认交割单边界解析和重复检测结果
- 动态确认旧附件目录和备份压缩；同步设置与外围页面已完成，继续验证真实同步冲突/删除传播/断点续传、移动端结果和其余行情源
- 对不进入产品范围的 AI/控制台能力保留排除依据和追溯记录
- 校准 19 类财务计算公式、账单日短月份/历史规则、日记搜索导出、附件文件生命周期和价格整理预览/回滚

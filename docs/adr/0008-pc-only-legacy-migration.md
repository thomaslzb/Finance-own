# PC-Only Legacy Migration

Finance Own will run first-version MoneyHome8 legacy migration only on the PC client. Legacy source files stay local, Rust converts them into new Finance Own objects and writes PC SQLite, and only confirmed new-system objects are synchronized to the cloud and other clients.

# Ledger Membership Before Fine-Grained Permissions

Finance Own will include a ledger membership model from the first-version data design, with at least owner, editable member, and read-only member roles. Fine-grained permissions by account, category, amount, or similar dimensions are intentionally deferred to keep the first version focused while avoiding a single-user-only data model.

# SQLite for PC Local Ledger

Finance Own will keep SQLite as the PC local ledger store. SQLite fits complete offline writing, single-machine ledger files, transactional safety, backup/export workflows, and the existing Rust migration work, while synchronization uploads object changes and batches rather than whole SQLite database files.

# PostgreSQL for Cloud Service

Finance Own will use PostgreSQL as the first-version cloud database behind the .NET service. PostgreSQL fits object synchronization, audit logs, conflict records, JSON extension fields, flexible cloud deployment, and self-hosting better than SQL Server for the initial personal finance product scope, while PC local storage remains SQLite.

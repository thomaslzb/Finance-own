# Owned Account System

Finance Own will use an owned account system in the first version, managed by the .NET service for users, sessions, refresh tokens, and devices. PC can be used locally without login and sync after login, while Web requires login; third-party login, enterprise SSO, and platform-specific identity providers are deferred.

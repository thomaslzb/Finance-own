# Explicit Confirmation Before PC Ledger Sync

Finance Own will require explicit user confirmation before a PC local ledger is bound to an account and uploaded for cloud synchronization. Logging in alone must not upload local financial data, because personal finance records are sensitive and users may intentionally keep a ledger local-only.

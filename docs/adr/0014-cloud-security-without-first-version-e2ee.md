# Cloud Security Without First-Version E2EE

Finance Own will not require full end-to-end encryption in the first version. The cloud service must provide HTTPS transport security, encryption at rest, key management, ledger-level authorization, and sensitive-field protection, while full E2EE is deferred because it complicates Web querying, password recovery, shared ledgers, server-side conflict handling, and multi-device restore.

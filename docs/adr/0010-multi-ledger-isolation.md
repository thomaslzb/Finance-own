# Multi-Ledger Isolation

Finance Own will support multiple ledgers from the first-version data model, and ledger data must be isolated across local storage, cloud storage, synchronization, permissions, attachments, cache, and reports. Every financial object belongs to an explicit ledger, avoiding a later costly retrofit of ledger scoping.

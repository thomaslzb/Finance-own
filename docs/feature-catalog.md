# MoneyHome8 功能目录

本文档把当前已识别的功能点统一整理为一份可核对的功能目录。全量机器审计见 [runtime-form-coverage-audit.md](C:\DCG-SZ\SZ-System-Docs\CodexWorkSpace\Finance-own\docs\runtime-form-coverage-audit.md)：`460/460` 个运行时窗体已归类，包含 `400` 个直接业务表面、`37` 个父窗体驱动的嵌入视图、`2` 个内部或实验入口和 `21` 个技术支撑组件，未分类数为 `0`。组合审计见 [runtime-form-composition-evidence.md](C:\DCG-SZ\SZ-System-Docs\CodexWorkSpace\Finance-own\docs\runtime-form-composition-evidence.md)：`37/37` 个嵌入视图均已定位直接父窗体和最终宿主。事件代码审计见 [runtime-event-handler-evidence.md](C:\DCG-SZ\SZ-System-Docs\CodexWorkSpace\Finance-own\docs\runtime-event-handler-evidence.md)：`2332` 条 DFM 事件绑定归并为 `2000` 个按窗体去重处理器，其中 `1951` 个已定位当前类或真实父类代码。特殊表面结论见 [runtime-internal-surface-evidence.md](C:\DCG-SZ\SZ-System-Docs\CodexWorkSpace\Finance-own\docs\runtime-internal-surface-evidence.md)。

状态标记：

- `实测`
  - 已有真实页面截图或当前运行界面证据
- `高可信`
  - 已由资源窗体、配置文件、数据对象、缓存或参考库多源支撑
- `待验证`
  - 方向明确，但还缺真实页面或正式数据结构验证

## 1. 顶层工作区

- `实测` 财务数据
- `实测` 财务报表
- `实测` 财务分析
- `高可信` 记账

## 2. 账本与系统

- `高可信` 新建账簿
- `高可信` 打开账簿
- `高可信` 关闭当前账簿
- `高可信` 备份账簿
- `高可信` 还原账簿
- `高可信` 系统设置
- `高可信` 快捷键管理
- `高可信` 密码校验
- `高可信` 密码修改
- `高可信` 关于页

## 3. 财务数据工作区

### 3.1 左侧导航

- `实测` 概况
- `实测` 财务记录
- `实测` 投资一览
- `实测` 标签
- `实测` 账户中心

### 3.2 账户中心

- `实测` 账户组树结构
- `实测` 新增账户组
- `实测` 新增账户
- `实测` 所有账户类型筛选
- `实测` 按账户类型查看
- `实测` 底部资产/负债/净资产汇总

### 3.3 已实测账户类型

- `实测` 现金
- `实测` 活期（卡折）
- `实测` 定期
- `实测` 第三方储值
- `实测` 上市证券
- `实测` 开放式基金
- `实测` 外汇
- `实测` 重大资产
- `实测` 应付款
- `实测` 预收款
- `实测` 应收款

### 3.4 开户向导家族

- `高可信` 现金账户向导
- `高可信` 活期账户向导
- `高可信` 定期账户向导
- `高可信` 第三方储值账户向导
- `高可信` 外汇账户向导
- `高可信` 证券账户向导
- `高可信` 开放式基金账户向导
- `高可信` 债权投资账户向导
- `高可信` 国债/债券账户向导
- `高可信` 货币理财账户向导
- `高可信` 信用账户向导
- `高可信` 双币信用账户向导
- `高可信` 保险账户向导
- `高可信` 社保账户向导
- `高可信` 黄金账户向导
- `高可信` 期货账户向导
- `高可信` 融资融券账户向导
- `高可信` 标准贵金属账户向导

## 4. 财务报表工作区

### 4.1 分组

- `实测` 日常收支类
- `实测` 资产负债类
- `实测` 投资类

### 4.2 日常收支类

- `实测` 日常收支表
- `实测` 日常收支明细表
- `实测` 账户日常收支表
- `实测` 标签日常收支表
- `实测` 两段时间收支对比表
- `实测` 收支统计表
- `实测` 收支走势图
- `实测` 月平均收支表
- `实测` 现金流表

### 4.3 资产负债类

- `实测` 资产负债表
- `实测` 可用资金表
- `实测` 债权债务表
- `实测` 月资产走势图

### 4.4 投资类

- `实测` 投资一览表、投资收益一览表和投资收益率统计表
- `实测` 证券投资一览表、证券费用及盈亏一览表和证券市值大势图
- `实测` 开放式基金投资一览表、开放式基金费用及盈亏一览表和开放式基金市值大势图
- `实测` 外汇交易一览表
- `实测空结果` 网贷盈亏一览表、银行理财产品收益率表
- `已动态确认` `12` 张投资类子报表的左树排列、结果列、分组小计、趋势图或明确无数据状态

### 4.5 报表公共交互

- `实测` 查询
- `实测` 筛选
- `实测` 操作
- `实测` 日期范围选择

## 5. 财务分析工作区

### 5.1 左侧导航

- `实测` 财务预算
- `实测` 财务诊断
- `实测` 财务规划
- `实测` 财务目标

### 5.2 预算

- `实测` 预算空状态
- `实测` 新增预算入口
- `实测` 预算列表、预算额与实际支出对比
- `实测` 创建月度/季度/年度预算
- `实测` 单元格、整列和跨年度预算金额复制；已确认十二个月默认全选、仅覆盖勾选月份、未来年度按上一年十二月初始化、非法年度拒绝和冷启动
- `实测` 编辑十二个月预算金额；已真实保存手工额度并确认最近十二个完整月份实际支出的导入、覆盖、月份映射和冷启动
- `实测` 编辑预算分类、信息和删除确认；已确认非零额度红色标记、移除分类清空全部月份额度、重新加入从零及冷启动
- `实测` 收入和支出分类共用金额网格，主页分别展示预算总收入和预算总支出，网格页脚另显示净额

### 5.3 财务诊断

- `实测` 财务诊断账簿统计/手工输入、八项金额冷启动持久化、资产性质设置和十二项指标公式/参考区间
- `实测` 定期资产 `103,501.97` 在流动/投资资产间等额重分类，总资产、负债和净资产不变
- `待验证` 自动统计日期范围、退款/冲销、跨币种与缺失估值，以及零分母、阈值端点、并发和失败回滚

### 5.4 财务规划

- `实测` 财务规划中心和年度余额图
- `实测` 规划基础信息、无配偶/有配偶分支和寿命年度范围
- `实测` 年收入信息
- `实测` 资产收入信息
- `实测` 资产支出信息
- `实测` 资产购置的一次性/分期字段及购置后收入/支出页签
- `实测` 家庭年日常支出信息
- `实测` 教育支出信息
- `实测` 其它收入信息
- `实测` 其它支出信息
- `实测` 支出调整信息
- `实测` 通货膨胀率信息
- `实测` 退休信息
- `部分实测` 年度数据结果：工资 `120000`、年支出 `60000`、零增长时每年净增 `60000`，冷启动保持
- `实测` 规划资产、债务和商业保险选择
- `待验证` 起始余额、通胀/退休/资产增长复利顺序、分期购置逐年展开、跨币种/舍入/负值、清除确认和失败回滚

### 5.5 财务目标

- `实测` 目标中心
- `实测` 目标创建、校验、删除和进度展示
- `实测` 目标账户选择
- `待验证` 多账户估值、日期边界和进度舍入口径

## 6. 记账工作区

- `高可信` 收入录入
- `高可信` 支出录入
- `高可信` 转账录入
- `高可信` 现金流水
- `高可信` 活期流水
- `高可信` 第三方储值流水
- `高可信` 流水账/日记账
- `高可信` 交易模板
- `高可信` 转账模板
- `高可信` 交易计划
- `高可信` 转账计划
- `高可信` 过滤/查找/日期范围
- `已确认结构` 记账公共录入、转账、分拆、模板、计划和流水命令；动态菜单与截图待验证

## 7. 债权债务与信用

- `高可信` 债权债务流水
- `高可信` 债权债务统计
- `高可信` 借入
- `高可信` 借出
- `高可信` 收款
- `高可信` 还款
- `高可信` 坏账
- `高可信` 债转股/权益互换
- `高可信` 利率设置
- `高可信` 信用卡流水
- `高可信` 信用卡统计
- `高可信` 信用提醒
- `高可信` 账单日/还款日管理
- `高可信` 应付款视图
- `高可信` 应付款预支/高级处理
- `高可信` 应付款资金流转
- `高可信` 应收款视图
- `高可信` 预付费用
- `高可信` 预付费用收支联动

## 8. 投资与扩展资产

### 8.1 外汇

- `高可信` 外汇流水
- `高可信` 外汇统计
- `高可信` 外汇兑换
- `高可信` 汇兑转账

### 8.2 证券

- `高可信` 证券列表
- `高可信` 普通买入
- `高可信` 市价买入
- `高可信` 委托买入
- `高可信` 卖出
- `高可信` 分红
- `高可信` 利息
- `高可信` 配额/配售
- `高可信` 证券统计
- `高可信` 证券提醒
- `高可信` 证券代码转换
- `高可信` 新股关联记录

### 8.3 开放式基金

- `高可信` 开放式基金列表
- `高可信` 申购
- `高可信` 赎回
- `高可信` 转换
- `高可信` 红利再投
- `高可信` 利息
- `高可信` 基金统计
- `高可信` 基金提醒

### 8.4 定期与理财

- `高可信` 定期流水
- `高可信` 定期统计
- `高可信` 到期处理
- `高可信` 货币/金融产品列表
- `高可信` 买入
- `高可信` 赎回
- `高可信` 到期
- `高可信` 理财统计

### 8.5 债券/国债

- `高可信` 国债/债券列表
- `高可信` 买入
- `高可信` 卖出
- `高可信` 提前支取
- `高可信` 到期
- `高可信` 利息

### 8.6 期货、黄金、贵金属、融资融券

- `高可信` 期货列表
- `高可信` 期货买入/卖出
- `高可信` 期货统计
- `高可信` 黄金列表
- `高可信` 黄金买入/卖出
- `高可信` 黄金统计
- `高可信` 标准贵金属列表
- `高可信` 标准贵金属买入/卖出
- `高可信` 标准贵金属统计
- `高可信` 融资
- `高可信` 融券
- `高可信` 利息还款
- `高可信` 融资融券统计

### 8.7 保险、社保、重大资产

- `高可信` 保险流水
- `高可信` 缴费
- `高可信` 领取
- `高可信` 保费转入
- `高可信` 保费转出
- `高可信` 现金价值维护
- `高可信` 分红
- `高可信` 结清/结束
- `实测` 保险缴费计划：仅提醒、固定账户自动计划、今日提醒跳过、类型化执行草稿、真实扣款、单期完成和冷启动自动入账
- `实测` 保险缴费计划维护：中央列表只读，保单上下文独立修改、五种频率菜单、两缴费年度半年重算、保存及冷启动回填
- `高可信` 社保流水
- `高可信` 社保统计
- `高可信` 重大资产买入
- `高可信` 重大资产变现
- `高可信` 重大资产增值
- `高可信` 重大资产投资
- `高可信` 重大资产其它费用
- `高可信` 重大资产价格维护
- `高可信` 重大资产估值管理

## 9. 导入导出与辅助工具

- `实测` 导入数据：21 类选择已确认，回导自身货币 XML 会触发旧程序崩溃
- `实测` 导入分类：候选查询、目标项目和确认替换结构已确认，真实替换仍待验证
- `实测` 导入交割单：系统方案、10 字段映射和证券买入预览已确认，最终提交仍待验证
- `实测` 导入预览：CSV 原始数据、规范化候选、默认选择和取消后无业务记录已确认
- `实测` 导入主题：筛选、候选列表、最终设置入口和临时标签清理已确认
- `实测` 导出数据：旧 XML 编码、版本、节点、最小成功和全量延迟完成已确认
- `实测` 附件对话框：日常收支草稿可达；空态下仅添加可用，删除、打开附件和打开文件夹禁用
- `高可信` 支票簿/票据相关对话框
- `高可信` 充值
- `高可信` 工资收入专用录入
- `高可信` 净价/清洁价格计算
- `实测` 财务计算器：存款 7 类、贷款 8 类、证券 1 类、其它 3 类，共 19 类；公式与边界待校准
- `实测` 共享金额计算器：日常收支金额字段按 `F4` 可达；`429` 个金额输入控件复用同一宿主，结果回填与错误文案待验证
- `实测` 日记/日记单位相关页：富文本编辑、粗体持久化、空值校验、修改、放弃未保存更改、搜索命中/空结果、查看全部、冷启动、月度计数、删除确认和日历投影已确认；导出只到目录选择器
- `实测` 价格整理：股票、基金、贵金属和汇率范围及日期/未交易对象条件已确认，未执行广泛删除
- `实测` 客户服务：邮箱、QQ、微博、网站、微信二维码、FAQ 和工作时间已确认，未触发外链
- `实测` 概况布局：六类图表、四个显示区块和五项拖动排序已确认
- `实测` 信用卡账单日管理：规则列表、生效日期、固定日和月末模式已确认，临时账户已删除

### 9.2 共享 UI 与技术支撑

- `实测` 金额筛选：作为财务记录完整筛选窗体的嵌入组件，默认不筛选金额，支持大于等于、小于等于和区间。
- `实测` 日期范围：支持日、周、月、季、年、最近 7 天、最近 1/3/6 月、最近 1 年和自定义起止日期。
- `代表性实测` 对话框、页签、树形下拉、日期下拉、进度、统计树/表格/工作区和详情投影均已从真实宿主确认，不建立同名独立页面。
- `动态宿主 + 静态合同` `TNodeWrapForm` 是本地 Web 内容包装层；Rust 版实现受限 Web 内容宿主，不复制旧 Node/wke 技术栈。
- `技术排除` `TRzFrmCustomizeToolbar` 无已知产品入口，属于第三方工具栏库内部定制器。
- `按需保留` `TfmCustomDialog` 只提供旧网格 And/Or 双条件筛选，不作为主导航功能。

### 9.1 内部与实验能力

- `动态排除 + 代码实证` 实验性 AI 面板：未发现用户入口；本地 `data/AIPanel.html` + JavaScript 桥 + 咨询/术语解释/FAQ 外部 HTTP 接口，因明文传输风险未触发
- `部分实测 + 代码实证` 内部控制台：快捷键设置显示 `Ctrl+F12`；三页、最近 10 条命令历史和 `25` 个诊断命令类由代码确认，当前会话未打开可见窗口
- `范围约束` AI 作为默认关闭的外部适配器；控制台只进入开发/诊断构建，不作为普通财务功能入口

## 10. 同步、提醒、外围入口

- `排除依据` `TLoginDialog` 是数据库驱动回退登录框，正常打开 `test.mh8` 和 B18 外部服务流程均未出现；Rust 版不提供面向用户的数据库凭据登录页
- `实测` 同步设置显示上次同步时间，默认双向同步，可切换单向上传、关闭账簿自动同步，并以用户条款勾选控制开始同步
- `实测` 同步注册包含电子邮箱、密码、确认密码和昵称；空表单提交提示输入电子邮箱
- `实测` 删除同步账号密码会二次确认；修改密码转向旧 `Password.aspx` HTTP 页面
- `实测` 手机快查设置复用同步账号并即时保存启用状态，注册入口复用同步注册窗体
- `实测` 行情更新包含九类最新来源、七类历史价格源、三种持仓范围和日期区间；外汇牌价成功写入 `28` 条，股票更新可在写入前中止
- `高可信` 更新验证码
- `实测` 财务日历默认当前日期、公历/农历、节日节气、账务记录标记、日记摘要及删除联动、公历生日摘要及人物删除联动、普通提醒待处理摘要及跳过后消失、十二个月份、`2026..1990` 年份范围、年月切换和只读重开；一个农历生日在对应农历日、原始月日和精确出生日期均未投影
- `实测` 计划与提醒列表、今日提醒及普通/收支/转账/基金四类计划；四类均有代表性保存与实例处理证据，三类交易计划还覆盖真实入账或周期跳过
- `实测` 限额提醒四类规则真实新增、默认启用、列表停用、账户修改/校验、账户与证券触发、基金不触发、证券删除及冷启动持久化
- `实测` 今日提醒统一投影计划、预算偏差和限额告警；限额告警执行/跳过均禁用，规则删除后当前告警立即重算
- `高可信` 日记入口

## 11. 仍未完全打通的关键缺口

- `待验证` `test.mh8` 正式认证与正式表结构
- `待验证` `记账` 动态菜单排列、页面跳转和真实写入结果
- `部分待验证` 财务诊断十二项公式和持久化已校准，仍缺自动统计范围、边界值与失败回滚；规划/目标仍缺完整公式和边界结果
- `部分待验证` 投资类报表的真实列、分组、小计、图表和空结果已动态确认，仍缺精确聚合公式、排序钻取、导出格式和打印结果
- `部分待验证` 通用 XML 和 CSV 预览已实测；成功回导、21 类完整映射、信用卡专用账单、备份压缩和附件目录仍待验证。同步与行情页面已完成 B18 动态取证，真实同步冲突/删除传播/断点续传、移动端结果及其余行情源成功失败路径仍待验证
- `部分待验证` AI 当前无用户入口；外部 HTTP 端点、发送字段、签名和响应协议已代码实证，Rust 版保留为默认关闭的安全适配器
- `部分待验证` 控制台快捷键配置已动态确认为 `Ctrl+F12`，三页数据方向和 `25` 个命令类已实证；仍缺可达前置条件、命令语法和真实副作用
- `部分待验证` `TCALCUFM` 已从 `TMHCalcuEdit` 通过 `F4` 动态唤起；仍缺结果回填、错误文案、焦点恢复和键盘关闭结果
- `待验证` `37` 个无独立文案嵌入视图在已确认父窗体中的数据来源、筛选、空状态和有数据结果
- `已形成决策` 三个无可执行 VMT 的资源均已有处理依据：交割单窗体保留，取款资源对应已动态到达的运行类，工具栏定制器按第三方内部能力排除

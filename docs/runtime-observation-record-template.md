# MoneyHome8 动态观察记录模板

每个 `runtime-execution-queue.json` 条目建立一份独立记录。结构化 JSON 必须符合
`runtime-observation-record.schema.json`，本模板用于同步记录人工观察和需求结论。

## 1. 基本信息

| 字段 | 内容 |
| --- | --- |
| `execution_id` |  |
| 资源类 / 页面标题 |  |
| 业务域 / 角色 |  |
| 观察时间 |  |
| MoneyHome8 版本 |  |
| 主窗口标题 |  |
| `test.mh8` 操作前 SHA-256 |  |
| `test.mh8` 操作后 SHA-256 |  |
| 备份证据 |  |

## 2. 入口与页面

入口：

步骤：

1. 

页面结构、字段、默认值、页签、选项和空状态：

## 3. 状态验证

| 状态 | 已观察 / 不适用 / 阻塞 | 页面结果 | 证据 |
| --- | --- | --- | --- |
| 初始 |  |  |  |
| 空数据 |  |  |  |
| 有数据 |  |  |  |
| 校验失败 |  |  |  |
| 取消 |  |  |  |
| 失败或回滚 |  |  |  |

## 4. 命令验证

| 控件 / 命令 | 初始状态 | 触发方式 | 确认或错误提示 | 页面与数据结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

## 5. 数据流

输入：

读取：

写入：

派生结果：

文件、网络、剪贴板或外部程序副作用：

取消、失败与事务回滚：

## 6. 计算与口径

最小样例：

预期值：

实际值：

排序、分组、小计、舍入、日期边界和币种口径：

## 7. Rust 需求回填

应用命令：

领域规则：

查询投影：

适配器或文件协议：

验收标准变化：

## 8. 结果

- 状态：`pass / fail / partial / blocked / unreachable / excluded`
- 摘要：
- 剩余缺口：
- 回填文档：

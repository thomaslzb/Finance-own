# Finance Own 领域术语

## Web 端

也称网络端。第一版指在线访问的浏览器端，不承担离线主账本写入职责。第一版 UI 采用 Flutter Web。Web 端可以在线记录财务数据、上传到云端并同步查看其它端提交的数据。Web 端支持完整冲突解决。

## PC 端

第一版指用户本机安装运行的桌面端，是本地账本、旧账本迁移和高级维护能力的主要承载端。PC 端必须支持完全离线可写，本地账本采用 SQLite。第一版 UI 采用 Flutter Desktop。PC 端可以记录财务数据、上传到云端并同步下载其它端提交的数据。PC 端支持完整冲突解决。Rust 本地核心第一版只强绑定 PC 端，并随 PC 安装包内置，不要求用户单独安装 Rust。

## 手机端

第一版指面向移动设备的客户端，强调随手记账、查询和同步后的轻量管理能力。第一版 UI 采用 Flutter Mobile。手机端可记录财务数据、上传到云端并同步下载其它端提交的数据。手机端可支持离线记账草稿、本地待同步队列、最近查询缓存和附件临时缓存，但不保存完整本地账本，不承担完整账本维护、旧账本迁移、批量治理或复杂冲突处理。手机端只提示冲突并允许延后处理。

## .NET 服务端

第一版云端服务端主技术，负责账号、同步、完整的新系统财务对象副本、附件、Web 在线访问和后台任务。第一版云端数据库采用 PostgreSQL。云端可以直接接收 Web 端记录的财务数据，也可以接收 PC 端和手机端上传的变更，并向三端下发同步数据。云端是对象级多主同步的协调中心，不是覆盖 PC 本地账本的唯一真相源。同步采用双向同步、对象级版本合并和显式冲突解决；真正冲突不静默覆盖。云端不保存旧 MoneyHome8 原始文件。

## 云端安全

第一版不做完整端到端加密。云端必须提供 HTTPS 传输加密、服务端静态加密、密钥管理、账本级权限隔离和敏感字段保护。

## 账号

第一版采用自有账号体系，由 .NET 服务端管理用户、会话、刷新令牌和设备列表。PC 端可以未登录使用本地账本，登录后再同步；Web 端必须登录后使用。

## 开启同步

PC 本地账本默认只在本机。用户登录后，必须显式确认开启同步，系统才可以把本地账本绑定到账号并上传到云端。

## 账本

用户可以拥有多个账本。第一版数据模型、同步、权限、附件、缓存和报表都必须按账本隔离，财务对象必须属于明确账本。

## 账本成员

第一版数据模型预留账本成员和共享账本能力，至少区分所有者、可编辑成员和只读成员。复杂的账户级、分类级或金额级细粒度权限后置。

## Rust 本地核心

第一版只作为 PC 客户端内置本地模块，负责旧账本迁移、本地完整离线账本、导入导出和高风险财务处理；不作为第一版云端服务端主技术。

## 旧账本迁移

第一版仅在 PC 端本地执行。旧 MoneyHome8 原始文件不上传云端；迁移审计、迁移报告和脱敏摘要也只保存在 PC 本地，不同步到云端。迁移结果写入 PC 本地 SQLite，经用户确认后再按新系统对象同步到云端和其它端。

## PC 本地 API

PC 端 Flutter 通过稳定本地 API 调用 Rust 本地核心。Flutter 不直接访问 SQLite、不拼接 SQL，也不直接读取旧账本文件。
